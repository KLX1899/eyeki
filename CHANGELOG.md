# Changelog

All notable changes should be documented here. The format follows Keep a Changelog; versioning is not yet established.

## [Unreleased]

### Added

- User, contributor, AI-agent, architecture, development, roadmap, privacy, security, release, and repository-audit documentation.
- GitHub bug, feature, and pull-request templates.

### Changed

- Corrected the Makefile's D-Bus provider from `dbus-1` to `libsystemd`, matching the source's `sd-bus` API.
- Updated ignore rules so source packaging metadata can be tracked while build binaries and generated source archives remain ignored.

## Release history

Reliable historical releases cannot be reconstructed. The repository has no tags or published-release records in its local history. Preliminary Debian metadata asserts `1.0.0-1` and a local archive is named with `1.0`, but neither is sufficient evidence of a release; no historical version entry is fabricated here.
